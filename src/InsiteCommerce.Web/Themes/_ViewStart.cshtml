@using Insite.WebFramework.Mvc.Extensions
@{
    // for pages to work with the CMS shell they need to use _PageLayout for their layout.
    // This file could also exist at Themes/[ThemeName]/Views/Pages for each theme

    Layout = Url.ThemedLayout("_PageLayout");
}